{
  "name": "@kitce/react-use-fadeout-scroll",
  "version": "2.0.0",
  "description": "React hook to apply fadeout effect to scroll container",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "pnpm clean",
    "build": "pnpm prebuild && tsc",
    "docs": "typedoc src/main.ts",
    "prepublish": "pnpm build && pnpm docs"
  },
  "keywords": [
    "react",
    "hooks"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/kitce/react-use-fadeout-scroll"
  },
  "author": "kitce",
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^18.0.10",
    "react": "^18.1.0",
    "typedoc": "^0.23.10",
    "typescript": "^4.7.2"
  },
  "peerDependencies": {
    "react": "^16.8.0  || ^17.0.0 || ^18.0.0"
  }
}
